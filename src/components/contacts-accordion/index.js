import { connect } from 'react-redux';

import ContactsAccordion from './view';

//Экшны
import {

} from '../../actions/popup';

export function mapStateToProps(state) {
  return {

  };
}

function mapDispatchToProps(dispatch) {
  return {

  };
}

export default connect(mapStateToProps, mapDispatchToProps)(ContactsAccordion);