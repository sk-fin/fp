import { connect } from 'react-redux';

import HomeSlideTre from './view';

export function mapStateToProps(state) {
  return {

  };
}

function mapDispatchToProps(dispatch) {
  return {

  };
}

export default connect(mapStateToProps, mapDispatchToProps)(HomeSlideTre);